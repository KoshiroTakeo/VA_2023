using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace VR.Enemys
{
    public class DummyEnemy : MasterEnemy
{
        // Start is called before the first frame update
        void Start()
        {
            SetUP();
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
